name: CI 

on:
  push:
    branches:
      - staging
      - trying
  pull_request:
    branches: [main]
  workflow_dispatch:
jobs:
  ci_basic:
    name: CI First stage 
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - name: job name 
        run: |
          echo hello
  done:
    name: Done
    if: github.event_name == 'push' && github.ref == 'refs/heads/staging'
    runs-on: ubuntu-latest
    steps:
      - name: Done
        run: echo "Done!"